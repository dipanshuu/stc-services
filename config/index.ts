import app from './app';
import db from './database';
import settings from './settings';
import services from './services';
import config from './config';

export default [app, db, settings, services, config];
